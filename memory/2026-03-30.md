
## Security Hardening (06:50 UTC)
Ran full healthcheck per Freddy's request. Actions taken:
- **UFW firewall** installed and enabled: deny-by-default, only SSH (22) and port 8000 allowed
- **CUPS** (snap) stopped and disabled — port 631 freed
- **SSH hardened**: PasswordAuthentication=no, PermitRootLogin=prohibit-password, MaxAuthTries=3, LoginGraceTime=30
- **OpenClaw `--fix`**: tightened session file permissions to 600
- **Unattended upgrades**: already enabled

Deferred (needs maintenance window or Freddy decision):
- Docker port binding (PostgreSQL 5432, Redis 6379, Weaviate 8081 to 127.0.0.1) — firewall blocks external access in the meantime
- Small model sandbox config (nemotron-nano-12B, nemotron-120B with web tools)
- OpenClaw update to 2026.3.28 (needs gateway restart)
- Port 8000 (ChromaDB) — should this be allowed externally?

Backup: /etc/ssh/sshd_config.bak.20260330

## Sprint 3: Enrichment Pipeline (06:59 UTC)
Built full enrichment pipeline architecture and code:
- **chunker.py** — recursive semantic chunking (paragraphs → sentences, 400-600 tokens, overlap). Tested: 8648 chars → 5 chunks of ~430 tok.
- **entity_extractor.py** — LLM-based entity extraction (Nemotron Super, extracts people/projects/concepts/tools/orgs + topics + summary)
- **backlinker.py** — autonomous backlinking via Weaviate similarity + entity overlap. 3 tiers: auto-link (≥0.85), LLM confirm (0.72-0.85), skip (<0.72). 5 link types.
- **enricher_v2.py** — orchestrates: chunk → extract → embed → generate seed → store Postgres+Weaviate → backlink
- **models.py** — added SeedLink, Entity, SeedEntity tables
- Design doc: `docs/sprint3-enrichment-architecture.md`
- Killed unused ChromaDB container, removed port 8000 firewall rule

## BFL Image Timeout (07:10-07:27 UTC)
Freddy asked to see the BFL/Flux generation image sent earlier. Attempted ~5 times with openai/gpt-image-1 — model produced images but delivery failed/timeouts every time. Switched to Gemini (google/gemini-3.1-flash-image-preview) as fallback; generated and delivered successfully. Issue likely network/latency between openrouter and BFL endpoint. Can retry BFL model when service is more stable.
