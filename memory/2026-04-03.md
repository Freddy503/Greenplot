# 2026-04-03

## 11:15 UTC — Deep Code Review & Bug Fixes
Freddy asked for deep code review + garden search investigation. Spawned two sub-agents:
- **Code review**: found 23 bugs (3 critical, 5 high, 10 medium, 5 low)
- **Garden search investigation**: found root causes for "What should I get into next?" failure

### Bugs Fixed (commit 81fc637)
1. **Double message send** — form onSubmit + PromptBox onSubmit both firing, bypassing enrichment. Removed form wrapper, wired handleSubmit directly to PromptBox
2. **Stale authToken** — enrichWithGarden captured empty authToken via useCallback([], []). Now reads fresh from localStorage at call time
3. **msgTimes state mutation** — Direct mutation of useState object. Moved to useRef
4. **Duplicate Toaster** — Both layout.tsx and chat/page.tsx mounting Toaster. Removed from chat page
5. **Frontend tenant leak** — Direct Weaviate BM25 query had no tenant_id filter. Switched to backend-only search
6. **Enrichment data mapping** — search_seeds read from top-level fields but add_seed stored in metadata JSON. Added metadata fallback
7. **Incomplete _ensure_schema** — Was 8 fields, now 24 (matching actual Weaviate schema)
8. **Hardcoded BACKEND_URL** — 11 routes had hardcoded URL. All 20 now use process.env.BACKEND_URL
9. **No error boundaries** — Added error.tsx for chat, garden, login, and root routes

### Still Pending (not yet fixed)
- In-memory memory stores (cold start data loss) — needs backend persistence (significant refactor)
- Enrichment pipeline re-run (5/228 seeds enriched)

## 11:37 UTC — Architecture Image + README + Low-Prio Fixes (commit 52718b1)

### Architecture Image
- Generated system architecture diagram via image generation tool
- Saved to docs/architecture.jpg

### README Updated
- Full ASCII architecture diagram with all components
- Updated status: all features green, 228 seeds
- Current tech stack, project structure, cron jobs
- Removed stale status items

### Low-Priority Fixes
1. **Header hydration mismatch** — added `mounted` flag; initial defaults to 'G' during SSR, syncs to real nickname after hydration
2. **localStorage chat messages unbounded** — capped at 50 messages; fallback to 20 on QuotaExceeded
3. **Garden loading skeleton** — added `loading.tsx` with skeleton grid matching garden layout

## 11:31 UTC — Architecture Fixes (commit 76dfef4)
Freddy asked about the two architecture concerns from the code review. Fixed both:

### Auth Middleware
- Removed Supabase dead weight — app uses custom JWT from backend, not Supabase
- New middleware rejects unauthenticated requests to protected API routes (/api/seeds, /api/chat, /api/push, /api/debug, etc.)
- Debug endpoint now redacts backend URL in production, requires auth
- Public routes (login, register) pass through without auth

### Push Subscriptions
- Was: POST to /api/v1/thoughts → enrichment pipeline → Weaviate seeds (polluting knowledge base)
- Now: Dedicated POST /api/v1/push/subscribe backend endpoint
- Subscriptions stored in data/push_subscriptions.json (separate from thoughts)
- Added GET /api/v1/push/subscriptions for tenant-scoped retrieval
- Frontend subscribe route updated to use dedicated endpoint + pass Authorization header
- VAPID key moved to NEXT_PUBLIC_VAPID_KEY env var

## 05:00 UTC — Weaviate Watchdog
- Ran watchdog script, output clean (no issues).

## 05:01 UTC — Auto-seed Harvest
- First attempt failed (curl truncated mid-string).
- Retried successfully: 0 harvested, 0 sessions checked.

## 08:59 UTC — Feature Requests from Freddy (Voice)

Three feature requests for the frontend:

### 1. Cron Cadence Selector (Onboarding Flow)
- Let users pick how frequently cron drops fire (daily, twice a week, weekly, etc.)
- Show an overview of which cron jobs they'll receive at each cadence level
- Builds on the existing cron flow; users should see what to expect (e.g. "all jobs once a day", "twice a week", etc.)

### 2. "Create Image" Button on Reflections
- When a user inputs a mental thought / reflection, show a "Create Image" button
- Trigger DALL-E image generation to visualize what they just talked about
- Should ONLY appear for reflection-type messages, not regular chat messages

### 3. "Add to Garden" Button for Longer Discussions
- After a substantive conversation (long web search, research session, reflection chain), surface an "Add to Garden" button
- Reminds/prompts users to save insights to their Idea Garden
- Should trigger after sufficient context has been built up (not on short exchanges)

### 4. Google Calendar Integration
- Connect Google Calendar during or after onboarding
- Smart cron timing: deliver notifications/cron drops only when user has free time (based on calendar gaps)
- Feed calendar insights into conversations (context-aware: upcoming meetings, deadlines, travel)
- Integrate calendar data into Morning Spark briefing

### 5. Morning Spark — Location-Aware Weather
- Use the city/nickname from onboarding (Step 1 "Roots") for weather in Morning Spark
- If user is in Munich → show Munich weather, not generic
- Simple: pass `city` from profile to the cron job that generates Morning Spark

## 09:19 UTC — Feature 5 Committed: Location-Aware Briefing
- User model: added `city` column with auto-migration
- Register endpoint: accepts optional `city`, saves to DB
- `get_daily_briefing` tool: fetches weather from wttr.in for user's city
- `PATCH /api/v1/profile` endpoint for future city updates
- Onboarding passes city during registration
- Committed as: `feat: location-aware daily briefing (Feature 5)`

## 09:20 UTC — Feature 2 Built: BFL Image Generation
- Backend: `POST /api/v1/images/generate` — BFL FLUX.2 [pro] async flow
- Frontend: reflection detection heuristics (`reflection-detect.ts`)
- `CreateImageButton` component with loading/error states
- Chat page: shows "Visualize this idea" only on reflection responses
- Generated images render inline with prompt caption
- Committed as: `feat: BFL image generation for reflections (Feature 2)`

## 10:08 UTC — Deep Code Review
- Spawned sub-agent for thorough code review of all new features
- Found 8 bugs, fixed all:
  1. Garden harvest sheet: side effect in render → useEffect (infinite loop risk)
  2. Calendar callback: ignored backend status, always reported success
  3. BFL: didn't handle 402/429 errors, increased timeout to 60s
  4. Calendar OAuth: state parameter not validated (CSRF risk)
  5. Extract-insights: LLM call not wrapped in try/except
  6. BulkSeedItem: missing Field validation on title/content
  7. BFL timeout fixed via #3
  8. Harvest sheet: safe-area padding for mobile

## 10:21 UTC — Garden Empty Bug
- Root cause: Weaviate seeds with `content: null` (not missing, explicitly null)
- `dict.get("content", "")` returns `None` when key exists but value is null
- Pydantic SeedResponse expects `content: str`, rejects `None` → 500 → empty garden
- Fix: use `or ""` pattern for all Weaviate field accessors
- Also fixed: expired token handling in garden page (redirect to /login on 401)

## 10:31 UTC — UI Redesign
- Freddy requested: rectangular messages (not round bubbles), brighter text
- First attempt: changed theme to lighter dark — Freddy said keep dark green
- Final: kept original green theme, reduced bubble radius from 2rem to 0.75rem
- Tighter padding on messages
- Reduced global --radius scale

## Architecture Notes (for future reference)
- Frontend: Vercel auto-deploys on push to main (seedify-six.vercel.app)
- Backend: Docker on Hetzner, manual `docker compose up -d --build api`
- Weaviate: 228 seeds for contact@example.com (tenant 87959b2e)
- BFL API key: in openclaw-api/.env
- Token TTL: 24 hours, garden redirects to /login on expiry

## 11:01 UTC — Context Window Fix
- MiMo-V2-Pro has 1M context (confirmed via OpenRouter API + web search)
- OpenClaw was defaulting to 200k for unknown models
- Config updated: added model to openrouter providers with contextWindow: 1024000
- OpenRouter cache confirmed correct: contextWindow: 1048576
- Current session permanently overflowed (237k/200k) — needs fresh session
- Issue: session metadata bakes in context window at creation time

## 10:37 UTC — Garden Search Fix (deployed)
- Frontend /api/seeds/search tried localhost:8080 (Weaviate) — fails on Vercel
- Added backend fallback: POST /api/v1/seeds/search endpoint
- Frontend now tries direct Weaviate first, falls back to backend
- Backend search endpoint uses embed_text + weaviate_client.search_seeds

## 10:40 UTC — Deep Code Review Findings
1. Garden search broken on Vercel (no backend fallback)
2. Memory route uses ephemeral serverless state
3. Null content in Weaviate search tool (hit.get("content","") returns None)
4. Daily briefing never triggers proactively (tool exists but LLM only calls on request)
5. Fixed: added briefing context injection for new sessions in chat v2
6. Fixed: null-safe field access in search_seeds tool handler

## 16:29 UTC — Session Start + Voice Memos + Idea Garden Seeds
New session started (MiMo-V2-Pro). Freddy sent several voice memos:
1. **Slogan seed**: "Due to the garden, the green plot helps you write the ink" → planted in Notion
2. **Agentic engineering framework seed**: voice memo explaining his setup (voice memos, sparks, reflections, sub-agents, web research, instant deployment) → planted in Notion
3. **Auto-suggest prompts feature**: "Every time you log in, auto-add 3-4 suggestions based on your garden" → planted in Notion

## 16:38 UTC — LLM Knowledge Bases Insight Analysis
Freddy shared a detailed post about using LLMs to build personal knowledge bases (Obsidian wiki, raw/ directories, LLM compilation, Q&A, linting, output rendering). Asked what applies to his project. Generated a roadmap.

## 18:27 UTC — Concrete Roadmap
Created full GreenPlot roadmap from seeds + insight. Freddy renamed "Link Collection Hub" to just "Hub".

## 18:37 UTC — Hub & Wiki Tabs Implemented
### Frontend:
- `/links` page (Hub): add/search/star/filter/delete links with auto-fetch metadata
- `/wiki` page: browse compiled articles with categories, backlinks, search
- `/api/links/fetch` route: scrapes page title, description, keywords
- Updated bottom-nav.tsx and header.tsx with Hub and Wiki tabs
- Both pages use localStorage, ready for backend integration

### Specs:
- Detailed specs saved to `docs/link-hub-wiki-specs.md`

## 18:41 UTC — Backend Integration + Weaviate Schema
### Weaviate Schema:
- `Link` class: url, title, summary, domain, tags, favicon, og_image, raw_text, status, starred, connection_count, related_ids, garden_seed_id
- `WikiArticle` class: title, category, summary, content, source_seed_ids, source_link_ids, backlinks, status, health_score

### Backend API (FastAPI):
- `/api/v1/links`: POST (add + auto-fetch metadata), GET (list/search/filter), PATCH (update/star), DELETE, POST /bulk
- `/api/v1/wiki`: GET (list/search/category filter), POST /compile, GET /health, GET /suggestions, GET /:id, PATCH, DELETE, POST /:id/regenerate
- `beautifulsoup4` added to requirements.txt

### Frontend API Proxy Routes:
- `/api/links/route.ts`, `/api/links/[id]/route.ts`, `/api/wiki/route.ts`
- Both pages wired to backend with localStorage fallback

## 19:06 UTC — P1 & P2 Implementation
### P1.1 — Backlinks & Connection Detection:
- POST /api/v1/links/detect-connections: tag/domain overlap scoring
- GET /api/v1/links/{id}/related: fetch related items
- Weaviate Link schema: added related_ids field

### P1.2 — Garden Health Dashboard:
- GET /api/v1/garden/health: combined stats (links, seeds, wiki)
- Coverage percentages, orphan detection, stale items, top domains, suggestions
- Frontend: collapsible health card in Wiki tab

### P2.1 — Synthesis from Seed Clusters:
- Enhanced wiki compile with domain-based auto-clustering

### P2.2 — Chat Against Garden:
- POST /api/v1/garden/ask: word-overlap relevance + Nemotron LLM answer
- Frontend: expandable Ask Garden Q&A in Wiki tab

## 19:13 UTC — Deep Code Review: 8 Bugs Found & Fixed
1. LinkUpdate missing related_ids field
2. update_link handler didn't pass related_ids to Weaviate
3. settings.OPENROUTER_BASE_URL didn't exist in config
4. Added OPENROUTER_BASE_URL to config
5. garden_health.py syntax error (except block indentation)
6. Unused imports cleaned up
7. WikiArticle interface missing optional fields
8. API proxy routes returned 200 on backend error → now 503

## 19:22 UTC — Prompt Suggestions + LLM Synthesis
### Dynamic Prompt Suggestions:
- POST /api/v1/garden/prompt-suggestions: generates 4 contextual prompts from garden
- Chat page fetches on mount, falls back to static defaults
- Strategies: starred items, enriched-without-wiki, pending links, recent articles, general exploration

### LLM-Powered Wiki Synthesis:
- Wiki compile endpoint now calls Nemotron to synthesize structured articles
- Extracts title from LLM output, generates summary
- Graceful fallback to raw concatenation

## 19:24 UTC — Full Implementation of Remaining Features
### Export:
- GET /api/v1/wiki/:id/export — download article as .md
- GET /api/v1/wiki/export/obsidian — zip with wikilinks, folder structure, INDEX.md
- Export buttons in wiki page header

### Bulk Import:
- Import button in Hub page, dialog accepts URLs or Chrome bookmark JSON
- Parses bookmark tree recursively, batch creates up to 20 links

### Save to Garden:
- 🌱 button on every assistant message in chat
- One tap → creates seed via /api/seeds

### Auto-Trigger Connections:
- PATCH link with status='enriched' auto-runs connection detection (event-driven)

### Real Regenerate:
- POST /api/v1/wiki/:id/regenerate re-runs Nemotron synthesis on source links
- Updates article title, content, summary, timestamps in place

### Training Data Export:
- GET /api/v1/garden/export-training — JSONL of Q&A pairs from wiki + links

### Linting Auto-Fix:
- POST /api/v1/garden/lint — detects and fixes: empty summaries, missing tags, duplicates, orphans

### Specs:
- `docs/remaining-specs.md` — full specs for all features
- `docs/p1-p2-specs.md` — P1/P2 detailed specs
- `docs/link-hub-wiki-specs.md` — Hub & Wiki detailed specs

### All commits today:
- `917afbe` — Hub & Wiki tabs
- `c184bd4` — Rename to Hub
- `4f9ee8b` — Backend integration + Weaviate schema
- `d26c056` — P1 & P2 (connections, health, ask garden)
- `eb1c8e0` — 8 bug fixes
- `acb222d` — Dynamic prompt suggestions + LLM synthesis
- `7a0f755` — Export, bulk import, save-to-garden, regenerate, linting, training data
