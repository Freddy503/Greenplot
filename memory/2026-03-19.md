- User preference: When asking to run "the entire workflow" after an Enterprise AI digest, default target companies must come from the latest digest (e.g., Snowflake, Microsoft, etc.), not previously discussed CRM leads.
- User explicitly requested this should be remembered for future runs.
- Hard boundary from Freddy: Never access other chats and never contact people without explicit approval/consent.
- OpenTable booking preference: use connector approach (strider-opentable) and only choose reservations that do NOT require credit/debit card holds.

## CRM & Lead Workflow (Odoo + Notion)
- Built a standardized lead enrichment flow pushing to Odoo CRM (`test-claw` db) and Notion.
- Odoo credentials updated: password is `NeuOpenClaw`.
- Notion workspace set up with Lead Research, Playbooks, and SOPs databases.
- Enriched and ingested leads for: Clicks, n8n, ElevenLabs, Tacto, Celonis.
- Odoo automation script `odoo_lead_ingest.py` committed to workspace.

## Preferences & Boundaries
- Hard boundary established: Never access other chats and never contact people without explicit approval/consent.
- OpenTable preference: Use the `strider-opentable` connector and only book options that do NOT require a credit/debit card hold.
- Lead routing preference: When asked to run the "entire workflow" after a digest, default to targeting companies from the latest digest.
- Conversational: Occasionally use emojis like 🤠🥀🫶🏼🦾⚡️.

## System Changes
- Set up a recurring cron job for a twice-weekly Enterprise AI news digest (Tue/Fri 08:30 UTC).
- Changed primary LLM provider to Gemini (`google/gemini-3.1-pro-preview`) and updated the API key via `openclaw onboard`.
- Installed `chromium` (deb) on the host to fix browser launch issues.
